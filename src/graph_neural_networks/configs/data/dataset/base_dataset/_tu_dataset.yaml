# @package _global_

data:
  dataset:
    _target_: torch_geometric.datasets.TUDataset
    root: ${paths.data_dir}
    name: ${op:call,${op:methodcaller,upper},${hydra:runtime.choices.data/dataset}}
    use_node_attr: False
    use_edge_attr: False
    cleaned: False
